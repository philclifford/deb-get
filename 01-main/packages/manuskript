  DEFVER=1
get_github_releases "olivierkes/manuskript" "latest"
if [ "${ACTION}" != "prettylist" ]; then
    URL=$(grep -m 1 "browser_download_url.*\.deb\"" <<< $(sed "s/,/\n/g" "${CACHE_FILE}")  | cut -d '"' -f 4)
    # VERSION_PUBLISHED=$(cut -d '/' -f 8 <<< "${URL//v/}")
    # The tag is plain but the  version installed/published is in the format "1.5.2-1" 
    VERSION_PUBLISHED=$(cut -d '/' -f9 <<< "${URL/.deb/}" |cut -d '-' -f 2- )
fi
PRETTY_NAME="Manuskript"
WEBSITE="http://www.theologeek.ch/manuskript"
SUMMARY="A open-source tool for writers"
